import numpy as np

arr = np.array([1]) 
print(arr)

# use np arrays since they are faster and easier to do math with for arrays

# Indexing: like lists -> np.array[index]

# Slicing: like lists -> np.array[index start: index stop]

# Useful Functions: np.sqrt(), np.mean(), np.sum(), np.median(), np.min()

